import Link from "next/link";

const Navbar = () => {
  return (
    <div className="bg-darker py-3 px-5 w-full drop-shadow-md fixed">
      <Link href="/">Navbar</Link>
    </div>
  );
};

export default Navbar;
